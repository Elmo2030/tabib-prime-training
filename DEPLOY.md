# النشر

موقع ثابت بالكامل — **مفيش خطوة بناء**. الملفات في جذر المستودع هي الموقع نفسه.

## الصفحات المنشورة

| الملف | إيه ده |
| :--- | :--- |
| `index.html` | شرائح الجزء الأول |
| `part2.html` | شرائح الجزء الثاني |
| `toolkit.html` | دليل الأدوات — ملحق 01 |
| `audit-card.html` | بطاقة التدقيق — ملحق 02 |

`src/`
فيها مصدر الشرائح وسكربت البناء — **مش جزء من الموقع**، بس بتتنشر مع المستودع وموسومة
`noindex`.

---

## Cloudflare Pages — الإعداد الموصى به

من لوحة
`Cloudflare`
:

`Workers & Pages`
←
`Create`
←
`Pages`
←
`Connect to Git`

اربط المستودع
`Elmo2030/tabib-prime-training`
وحط الإعدادات دي بالظبط:

| الحقل | القيمة |
| :--- | :--- |
| Production branch | `main` |
| Framework preset | `None` |
| Build command | **اتركه فاضي** |
| Build output directory | `/` |

بعد كده أي
`push`
على
`main`
بينشر لوحده.

### الترويسات

ملف
`_headers`
في الجذر بيتقري أوتوماتيك من
`Cloudflare`
— مفيش إعداد إضافي.

`HTML`
مالوش قاعدة فيه عن قصد: السلوك الافتراضي في
`Cloudflare`
هو
`max-age=0, must-revalidate`
وده بالظبط اللي محتاجينه علشان أي نشر جديد يوصل للقاعة فورًا.

---

## Netlify — الإعداد البديل

`netlify.toml`
لسه موجود وشغّال لو رجعنا. نفس المنطق: نشر الجذر، بلا أمر بناء.

الملفان
`_headers`
و
`netlify.toml`
**ما بيتعارضوش** — كل منصة بتقرا بتاعتها وبتتجاهل التانية.

---

## بعد أي نشر — تحقّق سريع

1. افتح `part2.html` — لازم تعد **63 شريحة**
2. افتح `toolkit.html` — لازم تشوف **13 كارت أداة**
3. افتح `audit-card.html` ودوس **اطبع** — لازم تطلع 3 صفحات `A4`
4. جرّب واحدة منهم على **موبايل حقيقي**، مش بس على اللاب توب
